based on the latest migrations related to stocks @database/schema.txt tables, i need to create a new view with similar features as products (search filter, advanced search, list view, add items, edit items), this new view will be for: /inventory path, and it will display a  interesting list: this list is like a 2 level deep tree, the first level will display "inventories" information and the second level will display stocks information, the list by default just display's the first level, and each item list has an icon to unfold and display the stocks inside each inventory, the search should filer by inventories.product_id.name also by DEVICE_STOCK.imei, DEVICE_STOCK.ime2 DEVICE_STOCK.serial_number.


the new /inventory url just created is referencing the page @resources/js/pages/InventoryPage.tsx it should be part of the current layout as @resources/js/pages/inventory.tsx already is, maybe move the code from InventoryPage.tsx to inventory.tsx, and update the web.php setting to make /inventory similar to /products setup, the current layout @resources/js/layouts/app-layout.tsx contains all the pages in 2 layout modes: resources\js\layouts\app\app-header-layout.tsx and resources\js\layouts\app\app-sidebar-layout.tsx, make /inventory part of the @resources/js/config/navigation.ts also update the inventory view to follow same structure UX and style as @resources/js/pages/products.tsx.


 display the following information at inventory row: batch_id.entry_date, batch_id.expiration_date, place_id.name. Improve the ui to display the information by groups spread them in the row, display it nicely, the count should be a big number arranged to the right, add a selection checkbox at the start-left and remove the edit button, instead when checked 1 item in the list display a floating button similar to current add but for edit the current item selected.  